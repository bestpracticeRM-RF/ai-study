venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
mlflow/
# Мы исключаем папку mlflow/, так как это скачанный чарт, его лучше ставить через helm или хранить как субмодуль, 
# но для начала можем и оставить, если хочешь. Пока скроем лишнее.

# Claude Code local settings (плагины, не для общего репозитория)
.claude/settings.local.json

# Triton model binaries (в MinIO + regenerated by serving/prepare_models.py)
serving/model_repository/**/*.onnx
*.onnx

# real values с кредами — вне git (есть *.example.yaml)
mlflow-values.yaml
